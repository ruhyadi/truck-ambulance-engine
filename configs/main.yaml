# main configs

defaults:
  - _self_
  - engine: [yolov8_trt]
  - override hydra/hydra_logging: none
  - override hydra/job_logging: none

# override hydra output directory
hydra:
  run:
    dir: .
  output_subdir: null
